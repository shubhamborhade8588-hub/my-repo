pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                echo 'Cloning Stage'
            }
        }

        stage('Build') {
            steps {
                echo 'Build Stage'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploy Stage'
            }
        }
    }
}
